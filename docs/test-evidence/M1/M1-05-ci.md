# M1-05 Continuous Integration Evidence

Date: 2026-09-16  
Task: `M1-05`  
Result: PASS

## Outcome and scope

The repository now defines a GitHub Actions workflow for pull requests, pushes to `main`, and manual runs. It separates quality/unit checks, production build verification, live infrastructure integration, and browser smoke tests so failures identify the affected boundary.

Playwright exercises the production Next.js server in desktop Chromium and a Pixel 5 viewport. The smoke test verifies HTTP success, the document title, main heading/landmark, keyboard skip link, and that Phaser/canvas are not eagerly loaded into the invitation shell.

## Acceptance criteria satisfied locally

- CI installs the exact Node and pnpm versions declared by the repository and uses the frozen lockfile.
- Linting, formatting, strict type checking, unit tests, Drizzle metadata, Compose configuration, and runtime audit run in CI.
- The production build and client-bundle secret/infrastructure scan run in CI.
- PostgreSQL, Redis, and BullMQ are exercised against healthy GitHub service containers.
- Browser tests cover desktop and mobile viewports against the production server.
- Failed browser runs upload traces, screenshots, and the HTML report when available.
- Workflow permissions are read-only, actions are pinned to immutable commits, and superseded runs are cancelled.

## Tests and results

| Test | Result |
|---|---|
| `pnpm install --frozen-lockfile` | PASS; all 9 workspaces installed reproducibly |
| `pnpm quality` | PASS; 14 Node tests plus boundaries, lint, formatting, and strict type checks |
| `pnpm verify` | PASS; production build and client-bundle scan included |
| `pnpm test:e2e` | PASS; 2/2 Playwright projects (desktop Chromium and Pixel 5); Windows uses headed mode to avoid an upstream cleanup defect |
| `rhysd/actionlint:1.7.12` against `.github/workflows/ci.yml` | PASS; no findings |
| [GitHub Actions run 35068068329](https://github.com/kresnawijayaa/wedding-game/actions/runs/35068068329) | PASS; all four jobs completed successfully |

## Performance, accessibility, and security impact

- The smoke test permanently guards the initial no-game bundle boundary and essential semantic invitation access.
- Browser artifacts are retained only on failure for seven days, limiting routine storage use.
- CI uses least-privilege repository permissions and immutable action references.
- CI test credentials are disposable local-service values; no deployment secrets are required or logged.

## Files and modules changed

- Workflow: `.github/workflows/ci.yml`.
- Browser runner and tests: `playwright.config.ts`, `tests/e2e/foundation.spec.ts`, and `tests/tsconfig.json`.
- Root scripts/dependency lock: `package.json` and `pnpm-lock.yaml`.
- Operator documentation: `README.md`.

## Plan/ADR/CR updates

- `M1-05` moved to `DONE` after GitHub Actions run `35068068329` passed; `M1-06` moved to `READY`.
- No ADR or Change Request was required; the workflow implements the existing delivery boundary.

## Remaining risks and follow-ups

- Future workflow changes must preserve the four independent gates and attach comparable evidence.
- The legacy Chromium headless-shell download was unreliable locally; the suite intentionally uses the maintained full Chromium channel and installs it with `--no-shell`.
- Windows uses headed Chromium because an upstream Playwright/Chromium profile-permission defect can leave a successful headless run stuck during cleanup; GitHub's Linux runner remains headless. Track <https://github.com/microsoft/playwright/issues/42109> before removing the fallback.
